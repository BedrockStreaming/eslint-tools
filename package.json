{
  "description": "M6 WEB's ESLint packages",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint --cache packages/",
    "format": "prettier-eslint --write 'packages/**/*.js'"
  },
  "devDependencies": {
    "@m6web/eslint-plugin": "^2.0.0-beta.1",
    "lerna": "^2.9.1"
  }
}
